#
# Skeleton file for the Python "Hello World" exercise.
#

# takes a string <name>, if no string is given, assumes it as 'World', then returns a string "Hello, <name>!"
def hello(name='World'):
    return "Hello, {}!".format(name)
